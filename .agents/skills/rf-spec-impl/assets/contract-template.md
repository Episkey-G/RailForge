# Implementation Contract

- Scope must come from the planning contract and task contract.
- Lead writer is the only default source-code writer.
- Specialists and evaluators may write review or proposal artifacts only.
- All execution writeback must land under `.railforge/runtime/`.
