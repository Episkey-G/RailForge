# Spec Plan Boundary

- 规划阶段输出长期知识到 `openspec/changes/`、`docs/product-specs/active/` 与 `docs/exec-plans/active/`。
- 只有当 research blockers 已解析且 spec 已审批后，才能进入 backlog 与 planning contract 生成。
- 规划阶段发现新的阻塞歧义时，只允许写回问题、决策与运行态阻塞信息，不允许进入实现。
