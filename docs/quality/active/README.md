# Quality Reports

This directory stores durable quality summaries such as `final_review.json` for the active RailForge workspace.
Runtime review payloads, task traces, and hosted execution handshakes belong under `.railforge/runtime/`; only durable, change-level quality summaries stay here.
