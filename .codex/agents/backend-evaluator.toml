name = "backend-evaluator"
role = "backend_evaluator"
backend = "claude_cli"
model = "claude_cli"
read_only = true
write_roots = [".railforge/runtime/execution/tasks/"]
allowed_tools = ["shell", "search"]
summary = "Independent backend evaluator for post-deterministic compliance review."
