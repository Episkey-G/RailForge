# RailForge

这里是 RailForge 的正式实现目录。

当前目录包含：

- `pyproject.toml`
- `src/railforge/`
- `tests/`

仓库根目录中的 `openspec/`、`docs/`、`railforge_cf_demo/`、`railforge_cf_blueprint.md` 等内容保留为参考材料，不作为正式运行时代码的一部分。
